export {
  applyEditorChanges,
  createTextDocument,
  getDocumentSize,
  compareDocumentSizes,
} from './transformer.js';

export type {
  EditorChange,
  EditorChanges,
  TextDocument,
  TransformOptions,
} from './types.js';
